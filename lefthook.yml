pre-commit:
  parallel: true
  commands:
    backend-lint:
      root: "server/"
      run: golangci-lint run
    backend-format:
      root: "server/"
      run: gofumpt -w .
pre-push:
  commands:
    backend-security-check:
      root: "server/"
      run: gosec -exclude=G101 ./...
